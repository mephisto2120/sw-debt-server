language: java
sudo: required
services:
  - docker

script:
  -  mvn clean package

after_success:
  - docker build -t mephisto2120/sw-debt-server .
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login --username "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mephisto2120/sw-debt-server
deploy:
  provider: elasticbeanstalk
  region: eu-central-1
  app: sw-debt-server
  env: Swdebtserver-env
  bucket_name: elasticbeanstalk-eu-central-1-700196549790
  bucket_path: sw-debt-docker-server
  on:
    branch: feature/test_deploy
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY